backend:
  name: github
  repo: yang1776/wanjia-service  # 你的 GitHub 仓库地址
  branch: main                   # 你的主分支名称

media_folder: "assets/uploads"   # 后台上传图片保存到的文件夹路径
public_folder: "/assets/uploads" # 网页调取的图片路径

collections:
  - name: "blog"
    label: "博客文章"
    folder: "blog"
    create: true
    slug: "{{year}}-{{month}}-{{day}}-{{slug}}"
    fields:
      - {label: "标题", name: "title", widget: "string"}
      - {label: "发布日期", name: "date", widget: "datetime"}
      - {label: "封面图", name: "image", widget: "image", required: false}
      - {label: "正文内容", name: "body", widget: "markdown"}